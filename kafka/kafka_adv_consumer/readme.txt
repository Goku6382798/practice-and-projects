first create the kafka docker environment using the kafka_testingwith_simpleproducerandconfigs docker-compose up -d
Second run the kafka_adv_producer in the environment already created 
third run the kafka_adv_consumer inside the python environment
go to another terminal release the data simply http POST :8000/api/people count:=5

And check it will work 
